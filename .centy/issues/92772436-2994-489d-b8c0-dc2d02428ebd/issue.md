# TUI should show only relevant tabs based on context

Currently all sidebar tabs are visible regardless of state. Hide project-specific tabs (issues, docs, assets, config) when no project is selected. Show them only after a project is selected.
